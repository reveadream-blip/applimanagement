{
  "name": "applimanagement",
  "private": true,
  "description": "Pages statiques + Functions (functions/). Deploy Git : wrangler pages deploy — pas wrangler deploy.",
  "scripts": {
    "pages:deploy": "wrangler pages deploy .",
    "pages:dev": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
